
PORT=10000
NODE_ENV=production

# Postgres (click “Add Database” in Render, then copy its connection string)
DATABASE_URL=postgres://...:5432/...?...sslmode=require
DB_SSL=true

# CORS & cookies
CORS_ORIGIN=https://<your-frontend-name>.onrender.com
SESSION_SECRET=<a-long-random-string>

# Optional (only if you use AI features)
OPENAI_API_KEY=sk-...
